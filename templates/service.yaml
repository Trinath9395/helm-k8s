apiVersion: v1 
kind: Service 
metadata: 
  name: nginx
spec: 
  type: LoadBalancer 
  selector: 
    app: nginx 
    tier: web 
    purpose: deployment 
  ports: 
  - protocol: TCP 
    port: 80 
    targetPort: 80 
    nodePort: {{ .Values.Service.nodePort }}